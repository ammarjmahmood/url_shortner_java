services:
  - type: web
    name: url-shortener
    env: java
    buildCommand: mvn clean package -DskipTests
    startCommand: java -jar target/url-shortener-1.0.0.jar
    envVars:
      - key: JAVA_VERSION
        value: 17
